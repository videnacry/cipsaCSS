import './enuncement.css'

export const Enuncement = () => {
    return(
        <>
        <h1>Ejercicio 1</h1>
        <p>A partir del c&oacute;digo HTML y CSS que se muestra, a&ntilde;adir los selectores CSS que faltan para aplicar los estilos deseados. Cada regla CSS incluye un comentaroi en el que se explica los elementos a los que debe aplicarse</p>
        </>
    )
}